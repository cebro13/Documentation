using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class ChainCrank : MonoBehaviour, ISwitchableLever
{
    [SerializeField] private float m_speedUp = 2f;
    [SerializeField] private float m_speedDown = 2f;
    //[SerializeField] private int m_maxLink = 15;
    [SerializeField] private CrankableChain m_chain;
    [SerializeField] private HandleGrabable m_handle;

    public void LeverRight()
    {
        //if(m_chain.GetNumberOfLinks() <= m_maxLink)
        //{
            m_chain.SetSpeed(m_speedUp);
            m_chain.SetIsAddingLink(true);
        //}
    }
    public void LeverLeft()
    {
        //if(m_chain.GetNumberOfLinks() > 1)
        {
            m_chain.SetSpeed(m_speedDown);
            m_chain.SetIsRemovingLink(true);
        }
    }

    public void LeverMiddle()
    {
        m_chain.SetIsAddingLink(false);
        m_chain.SetIsRemovingLink(false);
    }

    public void Grab()
    {
        m_handle.Grab();
    }

}
*/